name: Joint layout sampling experiment

on:
  push:
    branches:
      - agent/annealed-joint-layout-sampling-experiment

permissions:
  contents: write

jobs:
  write-token-smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo WRITE_TOKEN_WORKFLOW_OK

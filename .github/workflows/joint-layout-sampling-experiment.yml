name: Joint layout sampling experiment

on:
  push:
    branches:
      - agent/annealed-joint-layout-sampling-experiment

permissions:
  contents: read

jobs:
  snake-sampling:
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: uniform
            impl_share: "uniform"
          - label: impl35
            impl_share: "0.35"
          - label: impl50
            impl_share: "0.50"
          - label: impl70
            impl_share: "0.70"
          - label: impl85
            impl_share: "0.85"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/annealed-joint-layout
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Patch proposal sampler
        if: matrix.impl_share != 'uniform'
        env:
          IMPL_SHARE: ${{ matrix.impl_share }}
        run: |
          python - <<'PY'
          import os
          from pathlib import Path

          share = float(os.environ["IMPL_SHARE"])
          path = Path("src/factorio_circuit/synthesis/incremental_joint_layout.py")
          source = path.read_text()
          source = source.replace(
              "        movable_set = set(movable_objects)\n        if not movable_objects:\n",
              "        movable_set = set(movable_objects)\n"
              "        movable_relays = sorted(state.relay_positions)\n"
              "        if not movable_objects:\n",
              1,
          )
          old = "            object_id = movable_objects[rng.randrange(len(movable_objects))]\n"
          new = f'''            if movable_entities and movable_relays and rng.random() < {share!r}:\n                object_id = movable_entities[rng.randrange(len(movable_entities))]\n            elif movable_relays:\n                object_id = movable_relays[rng.randrange(len(movable_relays))]\n            else:\n                object_id = movable_entities[rng.randrange(len(movable_entities))]\n'''
          if old not in source:
              raise SystemExit("proposal selection site not found")
          path.write_text(source.replace(old, new, 1))
          PY
      - name: Install uv
        run: python -m pip install uv
      - name: Install dependencies
        run: uv sync --extra dev
      - name: Run 1000-proposal Snake layout
        run: |
          echo "BENCHMARK_VARIANT=${{ matrix.label }}"
          /usr/bin/time -f 'BENCHMARK_WALL_SECONDS=%e' \
            uv run python -m benchmarks.snake.generate \
              --annealing-layout \
              --annealing-iterations 1000 \
              --layout-retries 1 \
              --census \
              --output snake-annealed-blueprint.txt

# Benchmarks

Benchmarks will compare optimizer strategies by at least:

- physical combinator count;
- realized phases/latency;
- later: wire complexity, footprint, throughput.

The benchmark framework is intentionally postponed until multiple optimizer strategies exist.

D3 API surface is introduced in `factorio_circuit.synthesis.anchored_interface_routing`.

This small note exists only so the D3 draft can exercise documentation checks while the API is
stabilized. The durable design description is `milestone-d3-anchored-interface-routing.md`.
